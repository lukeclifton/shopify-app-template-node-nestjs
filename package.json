{
  "name": "my-node-app",
  "version": "1.0.0",
  "main": "web/backend/main.ts",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "prepare": "husky install"
  },
  "dependencies": {
    "@shopify/app": "^3.0.0",
    "@shopify/cli": "^3.0.0"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
